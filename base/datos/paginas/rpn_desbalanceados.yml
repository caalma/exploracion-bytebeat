---

titulo: 'EsB: RPN desbalanceados'
html_base: 'listado.html'
ruta_static: '/pag/rpn_desbalanceados/index.html'

contenido: |
  # RPN Desbalanceados { .py-4 }

lista_bb:
  - code:
      - t >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >>
      - t << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << <<
    show: true
    rate: 8000
    query_greggman:

  - code:
      - t << << << << << << << << <<
      - t << << << << << << << <<
      - t << << << << << << <<
    show: true
    rate: 8000
    query_greggman:

  - code:
      - t ~ + -
      - t ~ + - + -
      - t ~ + - + - +
    rate: 8000
    query_greggman: t=0&s=8000&e=1&bb=5d000001000d00000000000000003a080bc21141f7225d0cca1bfffff0f64000
    show: true

  - code:
      - t >> | >> | >> | >>
      - t >> | >> | >> | <<
    rate: 8000
    query_greggman: t=0&s=8000&e=1&bb=5d000001001400000000000000003a0803d82040547c037e0fffff841e0000
    show: true
