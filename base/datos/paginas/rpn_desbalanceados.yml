---

titulo: 'EsB: RPN desbalanceados'
html_base: 'listado.html'
ruta_static: '/pag/rpn_desbalanceados/index.html'

contenido: |
  # RPN Desbalanceados { .py-4 }

lista_bb:
  - code:
      - [t >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >> >>, 8000]
      - [t << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << << <<, 8000]

  - code:
      - [t << << << << << << << << <<, 8000]
      - [t << << << << << << << <<, 8000]
      - [t << << << << << << <<, 8000]

  - code:
      - [t ~ + -, 8000]
      - [t ~ + - + -, 8000]
      - [t ~ + - + - +, 8000]

  - code:
      - [t >> | >> | >> | >>, 8000]
      - [t >> | >> | >> | <<, 8000]
